# Tokenizer / 生成质量优化执行方案

> 制定日期：2026-09-01
> 依据：`TRAINING.md` 第 6 节实验记录 + 本文第 1 节的针对性诊断
> 目标：把采样质量从「有颜色有构图、无可辨识物体」推进到「可辨识物体、有纹理细节」
> 前置基线（实验 2/3）：val PSNR **18.8** · 码字使用 **6.15%** · 码本熵 **5.37 bit** · prior val_ppl **10.51**

---

## 1. 诊断：为什么现在效果不好

对 `runs/vqgan_full/ckpt/final.pt` 在 768 张验证图上做的量化诊断：

```
【1】码本使用        63 / 1024 = 6.15%
【2】码本向量范数    被使用的 8.987  |  未使用的 0.009      ← 相差 1015 倍
【3】编码器输出范数  13.853 ± 3.627
     未使用码字的范数只有编码器输出的 0.0006 倍
【4】量化误差        到最近码字距离 3.725 = 编码器范数的 26.9%
【5】码本熵          5.36 bit/token → 每图 171 字节
     若 1024 满用 = 320 字节 | 若 16384 满用 = 448 字节
【6】重建误差分解    低频 L1 0.0792 | 高频 L1 0.1000   ← 高频占 56%
```

### 1.1 根因 A：码本初始化尺度错误导致 961 个码字永久死亡

`models/vqgan/codebook.py:12`：

```python
self.codebook.weight.data.uniform_(-1/num_codewords, 1/num_codewords)
```

`num_codewords=1024` → 每维 ±0.00098，256 维向量的范数约 **0.009**。
而编码器输出的范数是 **13.85**。

后果是机械性的：最近邻搜索中，一个范数 0.009 的码字与任何范数 13.85 的输入的距离
≈ 13.85（几乎就是输入自身的范数），而早期偶然被选中的码字会被梯度推向数据分布、
范数长到 8.99，从此**永远赢下所有竞争**。961 个码字从初始化那一刻起就再没有被选中过，
也就再没有收到过任何梯度——它们被冻结在 0.009。

这解释了 `TRAINING.md` 里那条"使用率从 1.56% 缓慢爬到 6.15% 然后饱和"的曲线：
爬升来自少数边缘码字的偶然激活，饱和是因为剩下的码字在数值上已经不可能被激活。
**这不是训练不够，是初始化和缺少复活机制导致的结构性死锁。**

> `uniform(-1/K, 1/K)` 是 VQ-VAE 原论文对**低维**码本（如 64 维）的惯例，
> 在 256 维 + 无 EMA + 无死码复活的组合下会直接失效。

### 1.2 根因 B：即使码本满用，容量也不够

| 场景 | 每 token 比特 | 每图信息量 | 相对现状 |
|---|---|---|---|
| 现状（63 个有效码字） | 5.37 | **171 字节** | 1.0× |
| 1024 码本满用 | 10.00 | 320 字节 | 1.9× |
| 16384 码本满用 | 14.00 | 448 字节 | 2.6× |

原图 256×256×3 = 196,608 字节。即使修好坍塌、1024 码本 100% 利用，
压缩比仍是 **614:1**。LlamaGen 在 ImageNet 256 上达到 rFID 2.19 用的是
**16384 码本 × 8 维**，即 448 字节。**只修坍塌不够，还要扩容量。**

### 1.3 根因 C：只有 L1 损失，模糊是数学必然

诊断【6】显示重建误差的 **56% 在高频**。这不是容量问题——L1 损失优化的是
逐像素条件中位数，在信息不足时的最优解**就是**输出模糊的平均图像。
纹理、边缘、文字这些高频内容，L1 从原理上就不会去还原。

VQGAN 之所以叫 GAN，就是因为**感知损失 + 对抗损失**才是锐度的来源。
当前 `train_vqgan.py` 只有 `L1 + VQ`，`models/vqgan/` 里连判别器都没有。

### 1.4 三个根因的独立性

| 根因 | 决定 | 只修它的结果 |
|---|---|---|
| A 码本死锁 | 能否用满码本 | 信息量 171→320 字节，结构更准，**但依然模糊** |
| B 容量不足 | 信息上限 | 需先修 A，否则扩容只是增加更多死码 |
| C 损失函数 | 锐度/纹理 | 图更锐，**但结构受限于 171 字节，会锐化出错误细节** |

**三者必须同时修**。这也是本方案不做"性价比排序"、而是一次性做完整改造的原因。

---

## 2. 执行方案

### 阶段 T1 · 量化器重建（根因 A + B）

**文件**：新建 `src/models/vqgan/quantize.py`，替换 `codebook.py`

采用 LlamaGen / ViT-VQGAN 的成熟配方，四项改动缺一不可：

| 改动 | 现状 | 目标 | 作用 |
|---|---|---|---|
| **低维投影** | 直接在 256 维量化 | Conv1×1 投影到 **8 维**再量化，解码前投影回 256 | 低维空间里码字更容易覆盖数据分布 |
| **L2 归一化** | 无 | 码字与编码器输出都做 L2 归一化，用余弦距离 | 彻底消除"范数悬殊"这个死锁机制 |
| **码本容量** | 1024 | **16384** | 容量 10 bit → 14 bit |
| **EMA + 死码复活** | 无（纯梯度） | EMA(0.99) 更新；某码字连续 N 步未命中则用当前 batch 的随机编码器输出重置 | 从机制上保证不会有码字永久死亡 |

关键实现要点：

```python
# 1) 低维 + L2 归一化：距离计算等价于余弦相似度，范数不再影响竞争
z = F.normalize(self.proj_in(z_e), dim=1)          # [B,8,H,W]
cb = F.normalize(self.codebook, dim=1)             # [16384,8]
idx = torch.einsum('bdhw,kd->bkhw', z, cb).argmax(1)

# 2) EMA 更新（不走梯度，天然稳定）
self.cluster_size.mul_(decay).add_(onehot.sum(0), alpha=1-decay)
self.embed_avg.mul_(decay).add_(z_flat.T @ onehot, alpha=1-decay)
self.codebook.copy_(self.embed_avg / self.cluster_size.unsqueeze(1))

# 3) 死码复活：cluster_size 低于阈值的码字，用当前 batch 的编码器输出重置
dead = self.cluster_size < self.dead_threshold
if dead.any():
    self.codebook[dead] = z_flat[torch.randint(len(z_flat), (dead.sum(),))]
```

**新增监控**：`codebook_perplexity`（有效码字数的连续版本）、`dead_ratio`（本窗口内复活的码字数）。

**验收标准**：`val_codebook_usage > 50%`，码本熵 > 10 bit（每图 ≥ 320 字节）。

> **备选方案（若 VQ 调不稳）**：换 **FSQ**——投影到 6 通道、每通道量化到固定格点
> `[8,8,8,6,5]`=15360 码字。无码本参数、无 EMA、无复活逻辑、无 commitment loss，
> **结构上不可能坍塌**。代码量比现在还少，是稳妥退路。详见 `SCHEME_B_OPTIMIZED.md` B.2.2。

### 阶段 T2 · 编解码器主干升级（提升容量上限）

**文件**：`src/models/vqgan/vqgan.py`

当前主干是纯 `Conv + ReLU` 堆叠，**无残差块、无归一化层、无瓶颈自注意力**——
这三样是 VQGAN 系列的标准配置。

| 项 | 现状 | 目标 |
|---|---|---|
| 每级构成 | `Conv+ReLU` ×2 | **ResBlock ×2**（GroupNorm(32) → SiLU → Conv → skip） |
| 归一化 | 无 | GroupNorm(32) |
| 激活 | ReLU | SiLU |
| 通道 | `128→256→512→512→256` | base=128，`mult=(1,1,2,2,4)` |
| 瓶颈 | 无 | **16×16 处 1 层自注意力**（给全局一致性） |
| 上采样 | `ConvTranspose(4,s2)` | `nearest ×2 + Conv3×3`（消除棋盘伪影） |

**代价**：参数量 19.7M → 约 60M，吞吐 855 → 约 400 img/s（估计）。

### 阶段 T3 · 损失函数重建（根因 C）

**文件**：新建 `src/models/vqgan/losses.py`、`src/models/vqgan/discriminator.py`

```
L = L1 + λ_p · L_perceptual + λ_a(t) · L_adversarial + L_quant
```

| 项 | 权重 | 说明 |
|---|---|---|
| L1 重建 | 1.0 | 保留 |
| **感知损失** | 1.0 | 见下方骨干选择 |
| **PatchGAN 对抗** | 自适应 | **延迟到 ~20k step 后开启**，过早开启必崩 |
| 量化损失 | — | EMA 方案下只剩 commitment 项 |

**自适应对抗权重**（VQGAN 原论文做法，避免手调）：

```python
λ_a = ||∇_last_layer L_rec|| / (||∇_last_layer L_adv|| + 1e-4) * 0.8
```

**感知损失骨干的选择（本机网络受限，这一条很关键）**：

| 方案 | 权重来源 | 可用性 |
|---|---|---|
| **ResNet-50 特征匹配**（主选） | `~/.cache/torch/.../resnet50-19c8e357.pth` | ✅ **本地已有，已验证可加载，零下载** |
| 标准 LPIPS(VGG16) | `vgg16-397923af.pth` 528MB | ⏳ 已在后台下载，约 53 KB/s，ETA **2h48m** |

主选用本地 ResNet-50 的多层特征做 L1 特征匹配（`layer1~layer4`，各层归一化后等权）。
若 VGG16 下载完成，再切到标准 LPIPS 做一次对照。**不要因为等下载而阻塞 T1/T2。**

判别器：PatchGAN（4 层 stride-2 Conv + SpectralNorm + LeakyReLU），感受野 70×70，
hinge loss。约 2.7M 参数。

**验收标准**：高频误差占比从 56% 降到 40% 以下；重建图肉眼可见纹理与边缘。

### 阶段 T4 · 训练配置调整

| 项 | 现状 | 目标 | 理由 |
|---|---|---|---|
| `max_steps` | 40000 | **150000** | T1–T3 后模型更大、目标更难，需要更长训练 |
| `lr` | 1e-4 | 生成器 1e-4，判别器 1e-4 | |
| `disc_start` | — | 20000 | 对抗损失延迟开启 |
| `batch_size` | 64 | 32（显存换容量） | T2 后模型变大 |
| `ema_decay` | null | **0.999** | 采样用 EMA 权重 |
| 新增监控 | — | `perplexity` / `dead_ratio` / `d_loss` / `g_loss` / `λ_a` | |

### 阶段 P1 · Prior 结构修正（在 tokenizer 达标后进行）

Tokenizer 改好后，`vocab_size` 从 1027 变成 16387，需同步改 `constants.py`。
同时修正 `SCHEME_A_BASELINE.md` A.8 中列出的 prior 结构问题：

| 改动 | 现状 | 目标 |
|---|---|---|
| `n_heads × head_dim` | 24×128=3072 ≠ dim 2048 | **16×128 = 2048 = dim** |
| 注意力 | GQA 24:6 | **MHA**（seq 257 下 KV-cache 无意义，GQA 纯损质量） |
| FFN | SwiGLU ×4 → 12d² | **8/3 比例 → 8d²**（FFN 占比 76%→67%） |
| QK-norm | 无 | **q/k 加 RMSNorm**（视觉 token + fp16 的 logit 爆炸是已知高发问题） |
| 位置编码 | 1D RoPE | **2D RoPE**（h/w 轴各占 head_dim 一半） |
| 采样 | 无 KV-cache，O(n³) | **加 KV-cache** |

### 阶段 P2 · 条件化与 CFG

当前是**纯无条件生成**，这是"采样图无可辨识物体"的另一个独立原因——
模型要在零条件下拟合整个 LAION 的边缘分布。

| 项 | 方案 |
|---|---|
| 条件注入 | prefix token（条件向量投影到 dim，拼在序列首部） |
| 条件来源 | ALLaVA-LAION 自带 `ALLaVA-Caption-LAION-4V.json` **文本描述**，可直接做文生图 |
| 文本编码 | 优先用本机已有模型；否则退化为 K-means 伪类别标签 |
| 条件丢弃 | 训练时 10% 概率置为 null |
| 推理 | **CFG**，`s ∈ [2,4]` |

CFG 通常值数个 FID 点，是单项收益最大的技巧。**没有条件丢弃训练就做不了 CFG**，
必须在训练阶段就位。

> 📌 数据侧确认：`/home/dataset0/images/ALLaVA-4V/allava_laion/` 下有
> `ALLaVA-Caption-LAION-4V.json`，与图片一一对应，条件化不需要额外标注。

---

## 3. 对照实验协议

每次只改一个变量，其余固定：

```
data.limit=null  batch_size=64  max_steps=40000  lr=1e-4  warmup=1000  seed=0
```

比较三个数字 + 一张图：`val_psnr` · `val_codebook_usage` · 码本熵 · `samples/recon_40000.png`

| 编号 | 改动 | 预期 `usage` | 预期 `PSNR` |
|---|---|---|---|
| **E0** | 基线（已完成） | 6.15% | 18.8 |
| **E1** | 仅码本 8 维 + L2 归一化 | > 40% | > 19.5 |
| **E2** | E1 + EMA + 死码复活 | > 70% | > 20 |
| **E3** | E2 + 码本扩到 16384 | > 50%（基数变大） | > 21 |
| **E4** | E3 + 主干 ResBlock/GN/SiLU/Attn | > 50% | > 23 |
| **E5** | E4 + 感知损失 | 略降 | PSNR 可能略降但**观感大幅提升** |
| **E6** | E5 + PatchGAN | — | PSNR 再降，锐度显著提升 |

> ⚠️ **E5/E6 之后 PSNR 会下降，这是正常且预期的**。感知损失与对抗损失换取的是
> 人眼观感而非逐像素误差。从 E5 起，主要判据换成**重建图人工评估 + rFID**，
> PSNR 只作参考。这一点必须提前说清，否则会误判为"改坏了"。

配套新增：`scripts/eval_tokenizer.py`，一次性输出使用率、熵、每图字节数、
低频/高频误差分解、PSNR/SSIM，以及重建对比图——即本文第 1 节那套诊断的脚本化。

---

## 4. 资源与时间预算

单卡 A100 实测外推（依据 `TRAINING.md` 第 7 节）：

| 阶段 | 开发 | 单次 4 万步训练 | 备注 |
|---|---|---|---|
| T1 量化器 | 0.5 天 | 约 55 分钟 | 吞吐基本不变 |
| T2 主干 | 0.5 天 | 约 1.9 小时 | 参数 19.7M→60M，吞吐约 400 img/s |
| T3 损失 | 1 天 | 约 3.5 小时 | 判别器 + 感知网络前向 |
| T4 完整 15 万步 | — | **约 13 小时** | 最终 tokenizer |
| 预编码全量 48 万 | — | 约 5 分钟 | 已优化到 1650 img/s |
| P1 prior 修正 | 1 天 | — | |
| P2 条件化 | 1 天 | — | |
| Prior 训练 10 万步 | — | 约 25 小时 | 1.32B 单卡 |

**磁盘**：T2 后 tokenizer 约 60M 参数，checkpoint 约 0.7GB；prior 不变（16GB/个）。
建议 `run_dir` 指到 `/`（344G 空闲），不要放 `/home/dataset1`（仅剩 123G）。

**GPU**：GPU 0/2–7 长期被他人占用。T1–T3 的对照实验单卡即可；
最终的 15 万步 tokenizer 与 10 万步 prior 建议等到多卡可用时用 DDP 跑
（DDP 代码已就绪，见 `TRAINING.md` 第 8 节，但**尚未在真实规模验证过**）。

---

## 5. 风险与回退

| 风险 | 概率 | 处理 |
|---|---|---|
| EMA + 复活参数难调，码本仍不稳 | 中 | 回退到 **FSQ**，结构上不可能坍塌 |
| 对抗训练崩溃（判别器过强） | 中高 | `disc_start` 延后；自适应权重；判别器 lr 减半；最坏只保留 L1+感知 |
| VGG16 下载失败 | 中 | 主选就是本地 ResNet-50，不依赖下载 |
| 显存不足 | 中 | 降 batch + 提 `grad_accum`；`PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` |
| 16384 码本仍大量死码 | 低 | E2 已验证复活机制后再做 E3；必要时退到 8192 |
| 改 `vocab_size` 后旧 token 失效 | 确定 | 全量重新预编码（仅需 5 分钟），旧 `data/tokens_full` 作废 |

**回退点**：每个 E 编号都是一个完整可用的 checkpoint + 一组指标。
任何一步指标变差都可以停在上一个 E，不会丢失进度。

---

## 6. 执行顺序

```
第 1 步  写 scripts/eval_tokenizer.py（把第 1 节诊断脚本化）  ← 先有度量再改代码
第 2 步  T1 量化器  → 跑 E1、E2、E3 三轮对照（各约 55 分钟）
第 3 步  T2 主干    → 跑 E4（约 1.9 小时）
第 4 步  T3 损失    → 跑 E5、E6（各约 3.5 小时）
第 5 步  T4 最终 tokenizer 15 万步（约 13 小时）
第 6 步  全量重新预编码（5 分钟）+ 更新 constants.py 的 vocab_size
第 7 步  P1 prior 结构修正 + 训练
第 8 步  P2 条件化 + CFG + 训练
第 9 步  端到端评估：rFID / FID / 人工看图
```

**第 1 步必须先做**。当前只有 `val_psnr` 和 `val_codebook_usage` 两个指标，
不足以判断 T3 之后的改动是变好还是变坏（因为那时 PSNR 会下降）。
先把度量补齐，后面每一步的取舍才有依据。
