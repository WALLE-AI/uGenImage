# 方案 B：优化方案（Optimized）

> 对照文档：`SCHEME_A_BASELINE.md`（原方案）
> 设计原则：**把算力预算从 prior 挪回 tokenizer；废除自相矛盾的混合目标；一切结构决策为视频扩展预留接口。**

---

## B.0 设计前提与假设

以下两项尚未由项目方确认（`PLAN.md` 中的 D1/D5），本方案取**默认假设**并给出切换点：

| 假设 | 默认取值 | 切换代价 |
|---|---|---|
| **最终目标** | 图像先行，架构为视频预留接口 | 见 B.7 |
| **训练目标范式** | **掩码并行（MaskGIT/MUSE 式）** | 切回纯 AR 见 B.4-备选 |
| 数据规模 | ImageNet 量级（约 1.3M 图）→ 选 **B-S** 档；千万级 → **B-M** 档 | 见 B.3 规模阶梯 |

选择掩码并行的核心理由：**推理步数与序列长度解耦**（12 步 vs 256 步），这是方案 A 无法扩展到视频的根本症结。若项目重心是"统一多模态 LLM"，则应改选纯 AR。

---

## B.1 总体架构

```
图像 256×256  (或 视频 T×256×256)
  └─ Stage 1: FSQ Tokenizer  f=16  ──► 16×16 = 256 token   (视频: f_t=4 → T/4×16×16)
       └─ Stage 2: 双向 Transformer + 条件 prefix
            └─ 12 步并行置信度解码 (+CFG) ──► token ──► 解码器 ──► 图像/视频
```

相对方案 A 的四处范式级改动：

| | 方案 A | 方案 B |
|---|---|---|
| 量化 | VQ (1024×256d, 无 EMA) | **FSQ (15360 码字, 6 通道)** — 结构上不可能坍塌 |
| 训练目标 | 因果/掩码随机混合 | **纯掩码并行**（单一目标） |
| 条件 | 无 | **class/text prefix + CFG** |
| 推理 | 256 步 AR，无 KV-cache | **12 步并行解码** |

---

## B.2 Stage 1 — Tokenizer 重设计

### B.2.1 主干（替换 `models/vqgan/vqgan.py`）

| 项 | 方案 A | 方案 B |
|---|---|---|
| 通道配置 | `3→128→256→512→512→256` | base=128，`mult=(1,1,2,2,4)` → `128,128,256,256,512` |
| 每级构成 | `Conv+ReLU` ×2 | **ResBlock ×2**（GroupNorm(32) + SiLU + Conv + skip） |
| 归一化 | **无** | GroupNorm(32) |
| 激活 | ReLU | SiLU |
| 瓶颈自注意力 | **无** | **16×16 分辨率处 1 层自注意力** |
| 下采样 | 4× stride-2 conv | 同（f=16） |
| Decoder | 对称 ConvTranspose | 对称，上采样用 `nearest + Conv3×3`（消除棋盘伪影） |
| 输出激活 | Tanh | Tanh（输入 `Normalize([0.5],[0.5])`，值域一致） |

残差块 + 归一化 + 瓶颈注意力是 VQGAN 系列的标准配置，方案 A 三者全缺，是重建质量偏低的直接原因。

### B.2.2 量化器：FSQ 替换 VQ（`models/tokenizer/fsq.py`）

```
z_e [B,C,H,W]
  └─ 1×1 Conv: C → 6        (量化通道数)
       └─ bounded: z = (L_i-1)/2 · tanh(z)
            └─ round(z)  +  straight-through
                 └─ 码字 id = 各通道量化值的混合基数编码
```

| 项 | 方案 A (VQ) | 方案 B (FSQ) |
|---|---|---|
| 码字数 | 1024 | **15,360** = 8×8×8×6×5 |
| 量化维度 | 256 | **6**（每通道 levels `[8,8,8,6,5]`） |
| 可学习码本 | 1024×256 = 262K 参数 | **0 参数**（无码本） |
| EMA 更新 | 无 → 坍塌 | **不需要** |
| 死码复活 | 无 → 坍塌 | **不需要** |
| commitment / codebook loss | 需要 | **不需要** |
| 码本使用率 | 预计 < 20% | **结构上恒为 100%** |
| 距离计算 | `cdist`（显存重） | 无（逐通道 round） |

> FSQ 用固定网格替代可学习码本，**从机制上消除了码本坍塌**，同时删掉了 commitment loss、EMA、死码复活三套需要调参的机制。对小团队这是显著更稳的选择。
> 备选：若坚持 VQ，最低要求为 **码本 16384 × 维度 8 + L2 归一化 + EMA(0.99) + 死码复活**（LlamaGen 配方），不可沿用方案 A 的 1024×256。

### B.2.3 损失（方案 A 完全缺失的部分）

| 项 | 权重 | 说明 |
|---|---|---|
| L1 重建 | 1.0 | **方案 A 连这一项都没有** |
| LPIPS 感知 | 1.0 | VQGAN 画质的主要来源 |
| PatchGAN 对抗 | 自适应权重 | **延迟到 ~20k step 后开启**，过早开启会崩 |
| 量化辅助损失 | — | FSQ 不需要 |

### B.2.4 必须落地的观测指标

方案 A 无任何指标，tokenizer 好坏完全盲猜。方案 B 强制要求：

- **rFID / PSNR / SSIM**（重建质量，全链路硬上限）
- **码字使用率直方图 + 困惑度**（FSQ 下应恒满，用于验证实现正确）
- 每 N step 落盘 重建对比图

---

## B.3 Stage 2 — Prior 重设计

### B.3.1 结构改动对照

| # | 项 | 方案 A | 方案 B | 理由 |
|---|---|---|---|---|
| 1 | `n_heads × head_dim` | 24×128 = 3072 ≠ dim 2048 | **= dim** | 消除 Q/O 投影 50% 的无谓开销 |
| 2 | 注意力类型 | GQA 24:6 | **MHA** | seq=256 时 KV-cache 微不足道，GQA 纯损质量。视频阶段再启用 |
| 3 | FFN | SwiGLU expansion=4 → 12d² | **SwiGLU hidden ≈ 8/3·d → 8d²** | 回到标准比例，FFN FLOPs 降 33%；参数占比 76%→67% |
| 4 | QK-norm | 无 | **q/k 上加 RMSNorm** | 视觉 token + bf16 训练的 logit 爆炸是已知高发问题 |
| 5 | 位置编码 | RoPE 声明但**未接线**，且为 1D | **2D RoPE**，head_dim 按 h/w 轴对半分解 | 16×16 网格用 1D 序是错误先验；轴分解为 3D 预留 |
| 6 | 输出投影初始化 | 默认 | **× 1/√(2L)** | 20 层残差流方差累积 |
| 7 | RMSNorm 精度 | 输入 dtype | **fp32 upcast** | 避免 `x.pow(2)` 溢出 |
| 8 | 权重共享 | 绑定 | **解绑** | 见 B.3.2 参数核算 |
| 9 | 注意力接口 | `is_causal=(mask is None)` 耦合 | **`causal` 与 `attn_mask` 解耦** | 视频需要"时间因果 + 空间双向"混合掩码 |
| 10 | 条件化 | 无 | **条件 prefix token** | 见 B.5 |

### B.3.2 规模阶梯与精确参数核算

统一：`vocab = 15360 + 3 = 15363`（PAD / BOS / MASK），FFN hidden 对齐 256 的倍数。

**B-S（推荐起步档，ImageNet 量级数据）**

| 项 | 值 | 参数 |
|---|---|---|
| dim / layers | 768 / 16 | |
| heads × head_dim | 12 × 64 (MHA) | |
| FFN hidden | 2048 (= 8/3·768) | |
| Attention / 层 | 4 × 768² | 2,359,296 |
| SwiGLU / 层 | 3 × 768 × 2048 | 4,718,592 |
| 合计 / 层 × 16 | | 113,246,208 |
| Embedding + Head（解绑） | 15363 × 768 × 2 | 23,597,568 |
| 条件 embedding (1000类+null) | 1001 × 768 | 768,768 |
| Norms | | ~27,392 |
| **总计** | | **≈ 137.6M (0.14B)** |

**B-M（默认档，千万级数据）**

| 项 | 值 | 参数 |
|---|---|---|
| dim / layers | **1280 / 20** | |
| heads × head_dim | **16 × 80 (MHA)** | |
| FFN hidden | **3456** (≈ 8/3·1280) | |
| Attention / 层 | 4 × 1280² | 6,553,600 |
| SwiGLU / 层 | 3 × 1280 × 3456 | 13,271,040 |
| 合计 / 层 × 20 | 19,824,640 × 20 | 396,492,800 |
| Embedding + Head（解绑） | 15363 × 1280 × 2 | 39,329,280 |
| 条件 embedding | 1001 × 1280 | 1,281,280 |
| Norms（含 QK-norm） | | ~55,680 |
| **总计** | | **≈ 437.2M (0.44B)** |

> 注：方案 A 的 vocab 仅 1028，解绑只多 1.3M；方案 B 因码本扩到 15K，解绑代价升至 19.7M（占 4.5%）。
> **若需严格 ≤ 0.40B**：层数 20→18，总计 ≈ 397.5M。
> **对比方案 A 的实测 1.32B：B-M 参数量降 67%，且 FFN 比例回到 67%。**

**B-L（数据充足后放大）**

| 项 | 值 |
|---|---|
| dim / layers | 1792 / 24 |
| heads × head_dim | 16 × 112 |
| FFN hidden | 4864 |
| **总计** | **≈ 991M (0.99B)** |

### B.3.3 关于模型规模的判断

方案 A 用 1.32B 建模 256 个 token 的序列。若数据为 ImageNet 规模（1.3M 图 × 256 ≈ 3.3 亿 token），按约 20 token/参数的经验比例，**匹配容量约 150M** —— 方案 A 超配约 10 倍，会直接记忆训练集。

**因此建议：先用 B-S(0.14B) 把 tokenizer 与全链路打磨到位，确认 rFID 与采样质量后再放大到 B-M / B-L。** 在 tokenizer 未达标前放大 prior 是纯浪费。

---

## B.4 训练目标：纯掩码并行

废除方案 A 的随机混合目标，改为单一目标：

```
1. 从 U(0,1) 采 u，掩码率 r = cos(π/2 · u)        # 覆盖 0%~100% 全谱，非固定 0.15
2. 随机选 ⌈r·N⌉ 个位置置为 MASK_ID
3. 注意力全程双向（causal=False，无因果掩码）
4. 交叉熵损失只在被掩位置计算
5. 空掩码保护：若采样出 0 个掩码位置，强制至少掩 1 个
```

| 对照 | 方案 A | 方案 B |
|---|---|---|
| 目标数量 | 2 个（互相干扰） | 1 个 |
| 掩码率 | 固定 0.15 | cosine 采样，覆盖全谱 |
| 双向性 | 声明双向，**实为因果**（bug） | 真双向 |
| 训练/推理一致性 | 训练 20% 掩码，推理 100% AR → 不一致 | 完全一致 |
| 空掩码 NaN | 无保护 | 有保护 |

**备选：纯自回归**（若 D1 选择"统一多模态 LLM"路线）
- `causal=True` 固定，删除掩码分支
- 序列头写入 BOS（在 `preencode` 阶段，与 `constants.py` 统一）
- **必须实现 KV-cache**（方案 A 缺失，是推理慢约 100× 的原因）
- 其余 B.3 的 10 项结构改动全部适用

---

## B.5 条件化与 CFG（方案 A 完全缺失）

| 项 | 设计 |
|---|---|
| 条件注入 | **prefix token**：条件向量投影到 dim，拼在序列首部 |
| 图像阶段 | class embedding（1000 类 + 1 个 null 类） |
| 文本阶段 | T5/CLIP 文本特征 → 线性投影 → 多个 prefix token |
| 条件丢弃 | 训练时 **10% 概率替换为 null 条件** |
| 推理 | **CFG**：`logits = logits_uncond + s·(logits_cond − logits_uncond)`，`s ∈ [2, 4]` |

CFG 是图像生成中性价比最高的单一技巧，通常值数个 FID 点。**没有条件丢弃训练就无法做 CFG**，必须在训练阶段就位。

---

## B.6 训练配置

| 项 | 方案 A | 方案 B |
|---|---|---|
| 精度 | `torch.cuda.amp` fp16 + GradScaler（已废弃 API） | **bf16 autocast**（无需 GradScaler） |
| 优化器 | AdamW | AdamW，`betas=(0.9, 0.95)` |
| weight_decay | 配置缺失 → KeyError | 0.05，**排除 norm / bias / embedding** |
| LR 调度 | 创建了但**从不 step** → 恒定 | **按 step 计**：warmup 2000 步 → cosine 衰减至峰值 10% |
| 梯度裁剪 | 1.0 | 1.0 |
| 训练循环 | 按 epoch | **按 step**（与 warmup/cosine 相容） |
| 梯度累积 | 无 | 有（凑目标 batch） |
| EMA 权重 | 无 | decay 0.9999，采样用 EMA 权重 |
| 分布式 | `torch.distributed.launch` + **代码无 DDP** | `torchrun` + 真 DDP（B-L 时转 FSDP） |
| Activation ckpt | 无 | 有（放大时启用） |
| checkpoint | 仅 `state_dict` | `{model, ema, optimizer, scheduler, step, config}` + `--resume` |
| 随机种子 | 无 | 有，且记录到 checkpoint |
| 日志 | 无 | loss / lr / grad-norm / tok-per-s / 码字使用率 |
| 训练中采样 | 无 | 固定种子 + 固定条件，定期落盘 |
| 评估 | 无 | FID / IS（生成），rFID / PSNR（重建） |
| 配置来源 | argparse / sh / yaml **三处重复，yaml 无人读** | **YAML 单一来源** + CLI override |

---

## B.7 推理

**掩码并行解码（默认）**

```
tokens = [MASK] × 256
for t in range(12):                        # 步数与序列长度无关
    logits = model(tokens, cond)           # 双向，一次前向出全部位置
    logits = uncond + s·(cond − uncond)    # CFG
    pred, conf = sample(logits), confidence(logits)
    keep_n = N · (1 − cos(π/2 · (t+1)/12)) # cosine 调度
    保留置信度 top-keep_n 的预测，其余重新置 MASK
    conf 加 Gumbel 噪声，温度从 4.5 退火到 0
```

| 对照 | 方案 A | 方案 B |
|---|---|---|
| 步数 | 256（= 序列长度） | **12（常数）** |
| 每步前向 | 全量重算（无 KV-cache） | 一次全序列前向 |
| 总成本 | O(n³) | **O(12 · n²)** |
| CFG | 无 | 有 |
| 生成顺序 | raster 左上→右下 | **置信度驱动，粗到细** |
| 视频 4096 token | 4096 步 → 不可行 | **仍为 12–24 步** |

---

## B.8 视频扩展路径

方案 B 的结构决策已为视频预留接口，扩展时**只改配置与 tokenizer，不重写 prior**：

| 组件 | 图像阶段（现在） | 视频阶段（扩展） | 是否需重写 |
|---|---|---|---|
| Tokenizer | 2D FSQ, f=16 | **Causal 3D CNN + FSQ/LFQ**，时间 4× 因果下采样，首帧独立编码（图像/视频统一） | 需扩展（已预留 T 维接口） |
| 位置编码 | 2D RoPE (h, w) | **3D RoPE (t, h, w)** | 仅改 `axes_dim` 配置 |
| 注意力 | MHA + 全双向 | **GQA + 时间因果/空间双向混合掩码** | 仅改配置（接口已解耦） |
| 长序列 | 无需 | FlashAttention + 序列并行 | 新增，不改模型定义 |
| 训练目标 | 掩码并行 | **不变** | 无 |
| 推理 | 12 步 | **12–24 步（不随长度增长）** | 无 |
| 条件 | class prefix | text prefix + 首帧条件（I2V） | 复用 prefix 机制 |
| 评估 | FID / IS | FVD / CLIP-score / 时序一致性 | 新增 |

**与方案 A 的关键差别**：方案 A 的推理成本随视频长度**平方增长**（序列变长 × 步数变多），结构上无法扩展；方案 B 只有序列长度一项增长。

---

## B.9 两方案总览对照

| 维度 | 方案 A | 方案 B (B-M) |
|---|---|---|
| Tokenizer 量化 | VQ 1024×256d，无 EMA | FSQ 15360 码字 / 6 通道 |
| 码本坍塌风险 | **高（几乎必然）** | **结构上不可能** |
| Tokenizer 主干 | 纯 Conv+ReLU | ResBlock + GroupNorm + SiLU + 瓶颈注意力 |
| Tokenizer 损失 | 仅 VQ loss | L1 + LPIPS + PatchGAN |
| Prior 参数量 | 标称 0.39B / **实际 1.32B** | **0.44B（核算无误差）** |
| FFN 参数占比 | 76% | 67% |
| 注意力 | GQA 24:6，`n_heads·head_dim ≠ dim` | MHA 16×80 = dim |
| 位置编码 | 1D RoPE，**未接入** | 2D RoPE，轴分解 |
| 稳定性措施 | 无 QK-norm、无深度缩放初始化、LR 无 warmup 且不 step | 全部具备 |
| 训练目标 | 因果/掩码混合（且掩码分支实为因果） | 纯掩码并行 |
| 条件 / CFG | 无 / 无 | prefix / CFG |
| 推理步数 | 256 | **12** |
| KV-cache | 无（O(n³)） | 不需要 |
| 可评估性 | 无任何指标 | rFID / FID / 码字使用率 |
| 视频可扩展 | **否** | 是（仅改配置 + 3D tokenizer） |
| 当前可运行 | **否**（5 处崩溃 + 6 处设计缺陷） | — |

---

## B.10 落地顺序

方案 B 不应一次性实现，建议按下列顺序，**每步都有可验证产出**：

| 阶段 | 内容 | 验收 |
|---|---|---|
| 0 | `PLAN.md` 的 P0（去重、pyproject、冒烟测试） | `pytest` 可运行 |
| 1 | **Tokenizer 重写**（B.2 全部） | rFID / PSNR 达标，码字使用率满，重建图肉眼可辨 |
| 2 | Prior 结构改造（B.3 的 10 项） | 冒烟测试全绿，参数量核算一致 |
| 3 | 掩码目标 + 条件 + CFG（B.4/B.5） | 小数据集上 loss 稳定下降 |
| 4 | 训练底座（B.6） | 可断点续训、有日志、有定期采样 |
| 5 | B-S 完整训练 + FID 评估 | 产出可辨识内容的条件生成图 |
| 6 | 放大到 B-M / B-L | FID 随规模改善 |
| 7 | 视频扩展（B.8） | — |

**第 1 步是全局瓶颈**：tokenizer 未达标之前，第 2 步之后的一切改进都被 rFID 上限吃掉。方案 A 最大的资源错配正在于此 —— 82 行的 tokenizer 决定天花板，1.32B 的 prior 在天花板底下打转。
